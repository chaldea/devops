version: "3.7"
services: 
 nginx:
  image: nginx
  restart: always
  network_mode: bridge
  ports:
    - "433:433"
    - "80:80"
  volumes:
    - /opt/nginx/log:/var/log/nginx
    - /opt/nginx/html:/usr/share/nginx/html
    - /opt/nginx/conf:/etc/nginx/conf.d

# volumes: 
#   nginx_log:
#   nginx_html:
#   nginx_conf: